/* Escrever código reutilizável com funções. */

function funcaoReutilizavel () {
    console.log("Olá, Mundo!");
}

funcaoReutilizavel();

// 1 - Passagem de valores para funções com argumentos

/** 
     * parâmetros são variáveis locais listadas na definição de uma função que servem para receber os valores enviados durante a sua    chamada. 
     * 
     * Argumentos são os valores reais que passámos para a função quando vamos executá-la.
*/

function somarValores (a, b) {
    console.log(a + b);
}

somarValores(10, 20);

// Escopo Global



